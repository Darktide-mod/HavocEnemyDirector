# HavocEnemyDirector 发布流程

每次只发布本项目。进入本目录运行 ./Release.ps1；发布时无需生成图片。

1. 维护 src/HavocEnemyDirector 中的安装源码。功能变化时同步 info.json 和 publishing/metadata.json 的版本。
2. 在 publishing/release.json 设置 release_id。仅修改发布资料时使用“功能版本-r2”等发布修订号，不改变 mod 版本。已有发布目录不可覆盖。
3. 维护 description.en.md 和 description.zh-CN.md：英文与简体中文的长期功能说明，包含使用前提和既定安装方法。不要放版本号、更新日志、测试报告或发布过程信息。
4. 维护 summary.en.txt 和 changelog.en.txt。summary 为英文功能摘要，最多 350 字符；版本、变更和测试版说明放入英文 changelog。三语游戏界面和安装包中的文档照常保留。
5. 运行 ./Release.ps1。脚本先检查文案，再运行本项目检查，验证安装结构后生成 release/发布编号。检查记录保存在 build/checks，不复制到 release。
6. release/发布编号 内只有以下四个文件，没有子目录：

- HavocEnemyDirector-功能版本.zip：独立安装包，可直接上传 Nexus；顶层只有 HavocEnemyDirector/，没有嵌套压缩包。
- description.bbcode.txt：一份完整 BBCode 正文，英文在前，简体中文在后。
- changelog.en.txt：英文更新日志。
- summary.en.txt：英文简介。

7. 运行 ./Release.ps1 -Check 可校验已有发布。检查包括四文件清单、两种正文语言的顺序、BBCode 标签、英文文案、安装包内容及可用的 Vortex 安装器。
8. Nexus 的 Description 粘贴 description.bbcode.txt 全文；Short description 填入 summary.en.txt；Changelog 填入 changelog.en.txt；Files 上传 ZIP。正式包放入 Main Files，临时测试包使用 Optional Files，类别由 publishing/release.json 的 file_category 指定。

图片、HTML 预览、上传 JSON、中文／繁体 changelog 和额外提交说明均不进入新的发布目录。内部 publishing 中的配置及旧资料可保留，发布过程不读取图片。需要做界面开发时，可自行运行 RenderUI.ps1，其结果不随 release 发布。

安装包中的 info.json 等是模组必要文件，保留原样；发布脚本不以页面 description 替换安装包文档。Vortex 与手动安装方法见功能说明及安装包文档。

旧发布目录保留用于历史追溯及既有回归素材。后续只使用本流程生成精简产物。此流程不会部署游戏或上传 Nexus。

当前发布修订与功能版本：3.0.0，Main Files 正式发布。

文案仅说明本 mod 的命令和操作；其他 mod 仅在依赖或兼容性说明中提及。入口变化在 Nexus 正文中使用 [color=#ff6666]...[/color] 标红，并同步 DMF 帮助和安装说明。

正式源码完整移除 Debug：公共采样器、计时、统计、采样钩子、开关、翻译与对应说明。Main Files 自动执行完整移除与包内 Lua 编译检查，不依赖玩家关闭开关。必要错误处理和原版游戏限制保留。

只有 Optional Files 的显式移除变体使用 stripped_release_id 或默认功能版本-r2；正式新版本不会附加“测试状态未变”的旧打包说明。旧发布保留，当前检查入口是 tests/run.py。
